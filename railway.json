{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install cross-env && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node dist/index.js"
  }
}