{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && node init-data.js && node build-railway.js"
  },
  "deploy": {
    "startCommand": "node init-data.js && NODE_ENV=production node dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}